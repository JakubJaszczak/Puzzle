#include "board.h"

Board::Board(int _number_of_tiles,QSize buttonSize)
{
}

// std::vector<std::vector<QPushButton *> > Board::getBoard()
// {
//     return this->buttons;
// }

// void Board::setButton(int row, int col, QPushButton *button, QIcon buttonIcon)
// {
//     this->buttons[row][col]->setIcon(buttonIcon);
//     this->buttons[row][col]->setFixedSize(this->tileSize);
//     this->buttons[row][col]->setIconSize(this->tileSize);
// }

// Board::~Board()
// {
//     for (std::size_t i = 0; i < buttons.size(); ++i) {
//         for (std::size_t j = 0; j < buttons[i].size(); ++j) {
//             delete buttons[i][j];
//         }
//     }

// }
